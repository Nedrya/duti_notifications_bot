📋 Telegram Duty Bot

Бот для автоматических уведомлений о дежурствах на основе Google Sheets. Ежедневно отправляет информацию о том, кто сегодня ведущий и ведомый дежурный.
✨ Возможности

    📅 Автоматические уведомления в 10:00 по московскому времени

    🎨 Поддержка цветовой индикации в Google Sheets:

        🟢 Зелёный цвет — ведущий дежурный

        🔴 Другие цвета — ведомые дежурные

        🟡 Жёлтый цвет — отпуск (игнорируется)

    📊 Работа с производственным календарём — уведомления только в рабочие дни

    🛡️ Защита от флуда — не чаще 1 запроса в минуту для обычных пользователей

    👑 Админ-режим — без ограничений для администратора

    🧪 Тестовый режим — для отладки (отправка каждую минуту)

    🔗 Кликабельная ссылка на график дежурств в каждом уведомлении

    🔄 Автоматические повторные попытки при ошибках (до 5 раз)

Структура проекта
text

TelegramBotDuty/
├── src/
│   ├── bot.py                 # Основной файл бота
│   ├── config.py              # Конфигурация и переменные окружения
│   ├── google_sheets.py       # Работа с Google Sheets API
│   ├── handlers.py            # Обработчики команд Telegram
│   └── holiday_api.py         # API производственного календаря
│   └──service_account.json    # Ключи Google Sheets (не в git)
├── .env                       # Переменные окружения (не в git)
├── requirements.txt           # Зависимости Python
├── Dockerfile                  # Для Docker-образа
├── docker-compose.yml          # Для запуска в Docker
└── README.md                   # Этот файл

🚀 Быстрый старт (Предварительные требования)

    Python 3.12+

    Telegram Bot Token (получить у @BotFather)

    Google Sheets API credentials

    Google Таблица с графиком дежурств

Установка (Локальный запуск)

    Клонируйте репозиторий
    bash

    git clone <your-repo-url>
    cd TelegramBotDuty

    Создайте виртуальное окружение
    bash

    python -m venv .venv
    source .venv/bin/activate  # Linux/Mac
    # .venv\Scripts\activate    # Windows

    Установите зависимости
    bash

    pip install -r requirements.txt

    Настройте конфигурацию
    bash

    cp .env.example .env
    # Отредактируйте .env, добавьте свои токены

    Добавьте Google Sheets credentials

        Скачайте JSON-ключ сервисного аккаунта из Google Cloud Console

        Сохраните как service_account.json в корне проекта

        Дайте доступ этому email к вашей таблице

    Запустите бота
    bash

    python src/bot.py